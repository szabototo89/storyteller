import Application from '../src/application';
import { expect } from 'chai';

describe('hello', () => {
  it('world 1', () => {
    debugger
    expect(Application).does.exist;
  });

  it('world 2', () => {
    expect(Application).does.exist;
  });

  it('world 3', () => {
    expect(Application).does.exist;
  });
});
    